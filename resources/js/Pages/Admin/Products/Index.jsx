import Pagination from '@/Components/Pagination';
import AdminLayout from '@/Layouts/AdminLayout';
import { Head, Link, router, usePage } from '@inertiajs/react';

const formatRupiah = (value) => new Intl.NumberFormat('id-ID', { style: 'currency', currency: 'IDR', maximumFractionDigits: 0 }).format(Number(value));

export default function Index({ products, search = '' }) {
    const { flash = {} } = usePage().props;
    const submitSearch = (event) => { event.preventDefault(); router.get(route('admin.products.index'), { search: event.currentTarget.elements.search.value || undefined }, { preserveState: true, replace: true }); };
    const destroy = (product) => { if (window.confirm(`Hapus produk ${product.name}? Produk akan dipindahkan ke soft delete.`)) router.delete(route('admin.products.destroy', product.id)); };

    return <AdminLayout title="Produk"><div className="border-b border-slate-200 bg-white px-6 py-6 lg:px-10"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-center"><div><p className="text-sm font-semibold uppercase tracking-[0.18em] text-amber-600">Catalog management</p><h1 className="mt-2 text-2xl font-semibold text-slate-950">Produk</h1></div><Link href={route('admin.products.create')} className="rounded-lg bg-slate-900 px-4 py-2.5 text-center text-sm font-semibold text-white hover:bg-slate-700">Tambah Produk</Link></div></div><div className="mx-auto max-w-7xl px-6 py-8 lg:px-10">{flash.success && <div className="mb-5 rounded-lg bg-emerald-50 px-4 py-3 text-sm font-medium text-emerald-800">{flash.success}</div>}{flash.error && <div className="mb-5 rounded-lg bg-red-50 px-4 py-3 text-sm font-medium text-red-800">{flash.error}</div>}<form onSubmit={submitSearch} className="mb-6 flex max-w-xl gap-3"><input name="search" defaultValue={search} placeholder="Cari nama produk" className="min-w-0 flex-1 rounded-lg border-slate-300" /><button className="rounded-lg bg-slate-900 px-5 text-sm font-semibold text-white hover:bg-slate-700">Cari</button></form><div className="overflow-x-auto rounded-xl bg-white shadow-sm ring-1 ring-slate-200"><table className="min-w-full divide-y divide-slate-100 text-sm"><thead className="bg-slate-50"><tr>{['Produk', 'Kategori', 'Harga', 'Stok', 'Status', 'Aksi'].map((heading) => <th key={heading} className="px-5 py-3 text-left font-semibold text-slate-600">{heading}</th>)}</tr></thead><tbody className="divide-y divide-slate-100">{products.data.map((product) => <tr key={product.id} className={product.deleted_at ? 'bg-red-50/40' : ''}><td className="px-5 py-4"><div className="flex items-center gap-3">{product.thumbnail_path || product.image_path ? <img src={product.thumbnail_path || product.image_path} alt="" className="h-10 w-10 rounded-md object-cover" /> : <div className="flex h-10 w-10 items-center justify-center rounded-md bg-amber-100 text-[9px] font-bold text-amber-800">B</div>}<div><p className="font-semibold text-slate-900">{product.name}</p><p className="text-xs text-slate-400">{product.sku}</p></div></div></td><td className="px-5 py-4 text-slate-600">{product.category?.name ?? '-'}</td><td className="px-5 py-4 font-medium text-slate-900">{formatRupiah(product.price)}</td><td className="px-5 py-4 text-slate-600">{product.stock}</td><td className="px-5 py-4"><span className={`rounded-full px-2.5 py-1 text-xs font-semibold ${product.deleted_at ? 'bg-red-100 text-red-700' : product.status === 'active' ? 'bg-emerald-100 text-emerald-700' : 'bg-slate-200 text-slate-600'}`}>{product.deleted_at ? 'Deleted' : product.status}</span></td><td className="px-5 py-4"><div className="flex gap-3"><Link href={route('admin.products.edit', product.id)} className="font-semibold text-amber-700 hover:text-amber-900">Edit</Link>{!product.deleted_at && <button type="button" onClick={() => destroy(product)} className="font-semibold text-red-600 hover:text-red-800">Hapus</button>}</div></td></tr>)}</tbody></table></div><div className="mt-6"><Pagination links={products.links} /></div></div></AdminLayout>;
}